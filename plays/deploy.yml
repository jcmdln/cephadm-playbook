---
# deploy.yml

- name: Review defined hosts
  any_errors_fatal: true
  tags: always
  hosts:
    - mgrs
    - mons
    - osds
    - rgws
    - rgwlbs

  # pre_tasks:
  #   - name: Add Ceph repository to OSs that suck at life

  tasks:
    - name: Install Ceph packages
      package:
        name: "{{ ceph_packages }}"
        state: present
